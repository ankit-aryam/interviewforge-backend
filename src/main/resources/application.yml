spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/interviewforge
    username: postgres
    password: POSTWERT654TiTY
  data:
    redis:
      host: localhost
      port: 6379
  cache:
    type: redis

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true

server:
  port: 8080